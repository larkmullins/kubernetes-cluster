# Notes

## Etcd
- If other members are not running, `--initial-cluster` can cause etcd startup to fail.
- Across the net, it has been suggested that static bootstrapping is the preferred route to setup an etcd cluster.